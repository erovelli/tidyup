# tidyup-ui

Dioxus desktop UI for [tidyup](https://github.com/erovelli/tidyup). Consumes the same `tidyup-app` services as the CLI binary, with Dioxus-flavored implementations of the `ProgressReporter` and `ReviewHandler` ports.

Not published to crates.io.
