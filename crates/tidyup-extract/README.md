# tidyup-extract

Content extractors (PDF, Excel, images/EXIF, audio, plain text) for [tidyup](https://github.com/erovelli/tidyup). Each format is gated by a cargo feature so lean builds don't pay for formats they don't need.

Implements the `ContentExtractor` port from `tidyup-core`.
